import React from 'react';

// Construct types for component props
interface ResetLoanModalProps {
    
}

const ResetLoanModal: React.FC<ResetLoanModalProps> = () => {
    return (
        <React.Fragment>
            
        </React.Fragment>
    );
};

export default ResetLoanModal;